export interface ReduxIUser {
  orgId: string;
  user_id: string;
  role: string;
  profile:string;
  bio:string;
  planId: string;
  facebookAccessToken: string;
  instagramAccessToken: string;
  linkedinAccessToken: string;
  xAccessToken: string;
  email: string;
  name: string;
  organizationName: string;
  main_id:string
}
