"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.findCountryByIp = findCountryByIp;
const geoip_lite_1 = __importDefault(require("geoip-lite"));
const error_custom_1 = require("../errors/error.custom");
/**
 * Finds the geographical location details for a given IP address.
 * @param {string} ip - The IP address to look up.
 * @returns {Lookup | null} The geographical location details for the IP, or null if not found.
 * @throws {Error} If the IP address is invalid or not provided.
 */
function findCountryByIp(ip) {
    if (!ip) {
        throw new error_custom_1.CustomError("IP not found", 500);
    }
    return geoip_lite_1.default.lookup(ip);
}
